import { createStore } from 'vuex'
import ThemeModule from './modules/theme-module'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    ThemeModule
  }
})
